# Review Exporters

Profiles: canonical, GitHub, GitLab, CLI, and JSON. GitHub output includes summary, review Markdown, inline comments, and advisory patch guidance. GitLab output includes summary and discussions JSON. Exporters never alter finding severity, confidence, evidence, or source locations.
