# Phase 8 Assurance

Assured properties:

- exact contract-key matching;
- evidence from both endpoints;
- different source and target repositories;
- same-repository suppression;
- one-sided candidate preservation;
- ambiguous HTTP provider suppression;
- explicit inference classification for shared data;
- deterministic identifiers and ordering;
- atomic publication and tamper detection;
- no flagship-model dependency.

Evidence: `reports/graph-contract-report.json`, `reports/graph-test-report.json`, `reports/knowledge-cli-report.json`, and `reports/phase-8-summary.json`.
