# QnA Engine

`memory -> retrieval -> freshness/snapshot validation -> direct evidence verification -> answer synthesis -> critique -> citation validation`.

Answers are immutable under `qna/answers/<QNA-ID>/`. Generated documentation is never the sole evidence for implementation claims. Unknown and conflicting answers remain explicit.
